services:
  reakt-webkit:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - ADMIN_PASSWORD=demo
      - ADMIN_SESSION_SECRET=local-docker-session-secret
